<<<<<<< HEAD
name: Manual Build
=======
name: Build
>>>>>>> b8ba720 (added manual build action)

on:
  workflow_dispatch:
    inputs:
      tag_name:
<<<<<<< HEAD
        description: 'Image tag to use for the build (e.g., "v1.0.0" or "latest")'
        required: true
        default: 'latest'
      branch_name:
        description: 'Branch to build from'
        required: true
        default: 'main'
=======
        description: 'The tag to apply to the image (e.g., v1.2.3)'
        required: true
        default: 'latest'
>>>>>>> b8ba720 (added manual build action)

env:
  ARCHITECTURES: '["amd64", "aarch64"]'
  IMAGE_NAME: ha-airplay2
  CUSTOM_TAG: ${{ github.event.inputs.tag_name }}

permissions:
  contents: read

jobs:
  init:
    name: Initialize build
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v6
<<<<<<< HEAD
        with:
          ref: ${{ github.event.inputs.branch_name }}
=======
>>>>>>> b8ba720 (added manual build action)

      - name: Get build matrix
        id: matrix
        uses: home-assistant/builder/actions/prepare-multi-arch-matrix@2026.03.2
        with:
          architectures: ${{ env.ARCHITECTURES }}
          image-name: ${{ env.IMAGE_NAME }}

  build:
    name: Build ${{ matrix.arch }} image
    needs: init
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read # To check out the code
      id-token: write # Write needed for Cosign signing (issue OIDC token for signing)
      packages: write # To push built images to GitHub Container Registry
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.init.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v6
<<<<<<< HEAD
        with:
          ref: ${{ github.event.inputs.branch_name }}
=======
>>>>>>> b8ba720 (added manual build action)

      - name: Build image
        uses: home-assistant/builder/actions/build-image@2026.03.2
        with:
          arch: ${{ matrix.arch }}
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}
          image: ${{ matrix.image }}
          image-tags: |
            ${{ env.CUSTOM_TAG }}
          push: "true"
          version: ${{ env.CUSTOM_TAG }}

  manifest:
    name: Publish multi-arch manifest
    needs: [init, build]
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Write needed for Cosign signing (issue OIDC token for signing)
      packages: write # To push the manifest to GitHub Container Registry
    steps:
      - name: Publish multi-arch manifest
        uses: home-assistant/builder/actions/publish-multi-arch-manifest@2026.03.2
        with:
          architectures: ${{ env.ARCHITECTURES }}
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}
          image-name: ${{ env.IMAGE_NAME }}
          image-tags: |
            ${{ env.CUSTOM_TAG }}
